[pytest]
# Integration tests live at the stack level and exercise the installed packages
# together.  Both `.venv/bin/pytest` / `make test` and `python -m pytest` work:
# the rootdir conftest.py drops the repo root from sys.path so the source-tree
# clones (drm_screen/, drm_composer/, …) can't shadow the installed packages.
testpaths = integration
