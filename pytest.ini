[pytest]
# Integration tests live at the stack level and exercise the three installed
# packages together.  Run with the console script (`.venv/bin/pytest` or
# `make test`), NOT `python -m pytest` from the repo root: `-m` puts the cwd on
# sys.path, where the drm_screen/ clone shadows the installed drm_screen package.
testpaths = integration
